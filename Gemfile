source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins

group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-seo-tag"
end

# Windows에는 zoneinfo가 없어 timezone: Asia/Seoul 빌드에 필요
gem "tzinfo-data", platforms: [:mingw, :x64_mingw, :mswin, :jruby]
